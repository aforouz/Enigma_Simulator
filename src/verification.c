#include "config.h"

bool signup(char *username, char *password) {
	// mixing the username and password
	char *mixed = (char*) malloc(SIZE * sizeof(char));

	// encrypting username and password using Enigma algorithm

	// store this encrypted data to a cloud database or an internal DB
}


bool signin() {
	// mixing the username and password.

	// encrypting username and password using Enigma algorithm

	// resurrect the encrypted data and verifying the user.
}


int main() {

}